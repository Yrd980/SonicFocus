[project]
name = "sonicfocus-backend"
version = "0.1.0"
description = "Mock FastAPI backend for the SonicFocus Flutter MVP."
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.111.0",
    "uvicorn[standard]>=0.30.0",
]

[project.optional-dependencies]
dev = []

[tool.uv]
package = false
